{
  "name": "strong-env",
  "version": "1.1.0",
  "devDependencies": {
    "@types/node": "^12.7.3",
    "typescript": "^3.6.2"
  },
  "engines": {
    "node": ">=8"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "main": "dist",
  "repository": "callumlocke/strong-env",
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts --outdir=dist && bun tsc"
  }
}
