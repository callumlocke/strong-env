{
  "name": "strong-env",
  "version": "1.1.2",
  "devDependencies": {
    "@types/node": "^12.7.3",
    "typescript": "^3.6.2"
  },
  "engines": {
    "node": ">=8"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "main": "dist",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/callumlocke/strong-env.git"
  },
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts --outdir=dist && bun tsc"
  }
}
